import React, { Component } from 'react';

class App extends Component {
  render() {
    return (
      <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', height: '90vh', textAlign: 'center' }}>
        <div>
          <h1><code>Create-React-App</code> with <code>Redux</code>, <code>Node</code> and <code>Express</code> + <code>Google OAuth</code></h1>
          <h2>Modify <code>App.js</code> to get started!</h2>
          <h2>Open <code>server.js</code> to see how to configure <code>dev.js</code> file</h2>
        </div>
      </div>
    );
  }
}

export default App;
